apiVersion: v1
kind: Pod
metadata:
  name: ai-edtech-app
spec:
  containers:
  - name: app
    image: your-docker-image
    ports:
    - containerPort: 8000
    resources:
      limits:
        memory: "512Mi"
        cpu: "500m"